package model;

public class RegisterUserLogic {
  public boolean execute(User user) {
    // 登録処理（サンプルでは登録処理を行わない）
	  //↓テスト用処理
//	  System.out.println("登録内容→"+user.getId() + "," + user.getName() + "," + user.getPass());
    return true;
  }
}